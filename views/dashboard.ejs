<div class="dashboard-content">
  <!-- Messages -->
  <%- include('partials/messages') %>

  <!-- Header -->
  <div class="dashboard-header">
    <h1 class="dashboard-title">
      <% if (currentFolder) { %> 📁 <%= currentFolder.name %> <% } else { %>
      Welcome to Odin Drive <% } %>
    </h1>
    <p class="dashboard-subtitle">
      Hello, <span class="user-email"><%= user.email %></span>
    </p>
    <p class="dashboard-description">Your secure cloud storage solution</p>
  </div>

  <!-- Breadcrumb -->
  <% if (currentFolder) { %>
  <div class="breadcrumb">
    <div class="breadcrumb-item">
      <a href="/dashboard" class="breadcrumb-link">Home</a>
      <span class="breadcrumb-separator">/</span>
    </div>
    <div class="breadcrumb-item">
      <span><%= currentFolder.name %></span>
    </div>
  </div>
  <% } %>

  <!-- Action Buttons -->
  <div class="action-buttons">
    <button class="btn btn-primary" onclick="openCreateFolderModal()">
      <i class="button-icon">📁</i>
      New Folder
    </button>
    <button
      class="btn btn-primary"
      onclick="document.getElementById('fileInput').click()"
    >
      <i class="button-icon">📄</i>
      Upload File
    </button>
  </div>

  <%- include('partials/folder-section') %> <%-
  include('partials/upload-section') %> <%- include('partials/file-section') %>
</div>

<%- include('partials/modals') %>

<script src="/js/dashboard.js"></script>
